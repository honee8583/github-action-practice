name: CI/CD

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main", "dev" ]

env:
  DOCKER_HUB_USERNAME: ${{ secrets.DOCKER_HUB_USERNAME }}
  DOCKER_HUB_TOKEN: ${{ secrets.DOCKER_HUB_TOKEN }}
  IMAGE_NAME: ${{ secrets.DOCKER_HUB_USERNAME }}/yogi-odi-gym

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew build

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ env.DOCKER_HUB_USERNAME }}
          password: ${{ env.DOCKER_HUB_TOKEN }}

#      - name: Determine branch name
#        id: get_branch
#        run: |
#          echo "::set-output name=branch::$(echo ${GITHUB_REF#refs/heads/})";
#          branch=$( [ "${{ steps.get_branch.outputs.branch }}" = "main" ] && echo "main" || echo "dev" );

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ github.ref_name }}

#      # 3. Docker 이미지 빌드
#      - name: Build Docker image
#        run: |
#          TAG_NAME="latest"
#          if [ "${{ github.ref }}" == "refs/heads/main" ]; then
#            TAG_NAME="main"
#          elif [ "${{ github.ref }}" == "refs/heads/dev" ]; then
#            TAG_NAME="dev"
#          fi
#          docker build -t your-dockerhub-username/your-repo-name:$TAG_NAME .
#
#      # 4. Docker 이미지 푸시
#      - name: Push Docker image
#        run: |
#          TAG_NAME="latest"
#          if [ "${{ github.ref }}" == "refs/heads/main" ]; then
#            TAG_NAME="main"
#          elif [ "${{ github.ref }}" == "refs/heads/dev" ]; then
#            TAG_NAME="dev"
#          fi
#          docker push your-dockerhub-username/your-repo-name:$TAG_NAME